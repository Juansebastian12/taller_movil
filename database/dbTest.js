// import * as SQLite from 'expo-sqlite';

//const db = SQLite.openDatabase('test.db');

//db.transaction(tx => {
 // tx.executeSql(
   // 'CREATE TABLE IF NOT EXISTS prueba (id INTEGER PRIMARY KEY NOT NULL, nombre TEXT);',
    //[],
    //() => console.log('Tabla creada'),
   // (_, err) => console.log('Error', err)
  //);
//});
